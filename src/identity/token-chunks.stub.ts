/**
 * Dev placeholder for the stamped release token module.
 *
 * Dev builds/tests import this stub (STAMPED = false). Release builds alias
 * this specifier to the generated, gitignored `build/token-chunks.ts` — see
 * scripts/stamp-token.ts and the esbuild plugin in tsup.config.ts.
 */
export const STAMPED = false;
export const APP_ID = "";
export const EXPIRES_ON = "";
export const SALT = "";
export const CHUNKS: string[] = [];
